{
  "name": "cs2squad",
  "version": "2.0.0",
  "private": true,
  "description": "Competitive teammate discovery and team recruitment for Counter-Strike players.",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "test": "npm test --prefix server -- --runInBand && npm test --prefix client",
    "typecheck": "npm run typecheck --prefix client",
    "build": "npm run build --prefix client",
    "start": "npm start --prefix server"
  },
  "engines": {
    "node": ">=20"
  }
}
